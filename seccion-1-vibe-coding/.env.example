# Copiar este archivo a .env y completar con valores propios de desarrollo local.
# .env nunca se sube al repositorio (ver .gitignore).

# MySQL
MYSQL_ROOT_PASSWORD=changeme_root
MYSQL_DATABASE=tallerdae
MYSQL_USER=dev_user
MYSQL_PASSWORD=changeme_dev

# Puertos publicados al host
NGINX_PORT=80
ADMINER_PORT=8081
